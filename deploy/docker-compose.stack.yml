services:
  caddy:
    build: ./caddy
    container_name: caddy
    restart: unless-stopped
    dns:
      - 1.1.1.1
      - 8.8.8.8
    # Bind Caddy to BIND_IP (defaults to localhost).  Set it to your
    # Tailscale IP in .env to make the stack tailnet-only, or 0.0.0.0
    # to expose it publicly.
    ports:
      - "${BIND_IP:-127.0.0.1}:80:80"
      - "${BIND_IP:-127.0.0.1}:443:443"
      - "${BIND_IP:-127.0.0.1}:443:443/udp"
    environment:
      - CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN}
      - ACME_EMAIL=${ACME_EMAIL:-you@example.com}
      - DASHBOARD_DOMAIN=${DASHBOARD_DOMAIN:-agent.example.com}
      - N8N_DOMAIN=${N8N_DOMAIN:-flow.agent.example.com}
      - COUCHDB_DOMAIN=${COUCHDB_DOMAIN:-sync.agent.example.com}
      - ODYSSEUS_DOMAIN=${ODYSSEUS_DOMAIN:-odysseus.agent.example.com}
      - HOME_DOMAIN=${HOME_DOMAIN:-home.example.com}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
      - caddy_logs:/var/log/caddy
    depends_on:
      - dashboard
      - n8n
      - couchdb
      - home

  dashboard:
    build: ./dashboard
    container_name: dashboard
    restart: unless-stopped
    expose:
      - "8080"
    environment:
      - DASHBOARD_PASSWORD=${DASHBOARD_PASSWORD:-change-me}
      - JWT_SECRET=${JWT_SECRET:-insecure-default-change-me}
      - DASHBOARD_BASE_URL=${DASHBOARD_BASE_URL:-https://${DASHBOARD_DOMAIN:-agent.example.com}}
      - N8N_DOMAIN=${N8N_DOMAIN:-flow.agent.example.com}
      - COUCHDB_DOMAIN=${COUCHDB_DOMAIN:-sync.agent.example.com}
      - ODYSSEUS_DOMAIN=${ODYSSEUS_DOMAIN:-}
      - TIMEZONE=${TIMEZONE:-UTC}
      - CODE_AGENT_URL=${CODE_AGENT_URL:-}
      - CODE_AGENT_TOKEN=${CODE_AGENT_TOKEN:-}
      - LOQ_SFTP_URL=${LOQ_SFTP_URL:-}
      # NVIDIA_VISIBLE_DEVICES + runtime: nvidia let the dashboard's
      # /api/stats endpoint exec nvidia-smi for the GPU gauge.  Leave the
      # NVIDIA_* vars set to "" if you don't have a GPU — the container
      # will start fine without the runtime.
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=utility
    runtime: ${DOCKER_RUNTIME:-runc}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /tmp:/host-tmp:ro
      # Host path to this stack/ checkout — the dashboard chat reads
      # docker-compose.yml etc. from /host-stack inside the container.
      - ${HOST_STACK_PATH:-./}:/host-stack:ro
      - /:/host:ro
      - dashboard_data:/app/data

  n8n:
    image: docker.n8n.io/n8nio/n8n
    container_name: n8n
    restart: unless-stopped
    expose:
      - "5678"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      - N8N_HOST=${N8N_DOMAIN:-flow.agent.example.com}
      - N8N_PORT=5678
      - N8N_PROTOCOL=https
      - N8N_EDITOR_BASE_URL=https://${N8N_DOMAIN:-flow.agent.example.com}
      - WEBHOOK_URL=https://${N8N_DOMAIN:-flow.agent.example.com}
      - NODE_ENV=production
      - N8N_SECURE_COOKIE=false
      - GENERIC_TIMEZONE=${TIMEZONE:-UTC}
      - TZ=${TIMEZONE:-UTC}
    volumes:
      - n8n_data:/home/node/.n8n

  # Obsidian vault backend — Self-hosted LiveSync plugin replicates
  # against CouchDB over HTTPS at $COUCHDB_DOMAIN.  CORS + max-body-size
  # tweaks are applied at runtime by ./couchdb/init.sh via the HTTP
  # config API, not via a mounted local.ini.
  couchdb:
    image: couchdb:3
    container_name: couchdb
    restart: unless-stopped
    expose:
      - "5984"
    environment:
      - COUCHDB_USER=${COUCHDB_USER:-obsidian}
      - COUCHDB_PASSWORD=${COUCHDB_PASSWORD:-change-me}
    volumes:
      - couchdb_data:/opt/couchdb/data

  # Home hub — local device control (Haier AC over the LAN protocol on tcp/56800), plus
  # scenes and automations.  Bridge networking is deliberate: the container reaches the AC
  # through the host's routing table, so it needs no host network and Caddy can still find
  # it by name.  With no AC_LOCAL_KEY set it runs a simulated device instead of failing,
  # which keeps the UI usable before the key fetch has been done.
  home:
    build: ./home
    container_name: home
    restart: unless-stopped
    expose:
      - "8110"
    environment:
      - TIMEZONE=${TIMEZONE:-UTC}
      - HUB_DEMO=${HUB_DEMO:-0}
      - HUB_POLL_SECONDS=${HUB_POLL_SECONDS:-45}
      - AC_NAME=${AC_NAME:-Living Room AC}
      - AC_ROOM=${AC_ROOM:-Living Room}
      - AC_HOST=${AC_HOST:-192.168.1.36}
      - AC_DEVICE_ID=${AC_DEVICE_ID:-}
      - AC_LOCAL_KEY=${AC_LOCAL_KEY:-}
      - AC_TYPE_ID=${AC_TYPE_ID:-}
      - AC_SUBNET=${AC_SUBNET:-192.168.1.0/24}
      # Setpoint limits for this specific unit. Blank keeps the model profile's range
      # (the protocol-wide 16..30); this AC's own remote only allows 20..28.
      - AC_TEMP_MIN=${AC_TEMP_MIN:-}
      - AC_TEMP_MAX=${AC_TEMP_MAX:-}
      # Account used ONLY to re-fetch a rotated localKey. Haier rotates it server-side and
      # the trigger is undocumented, so the hub heals itself rather than waiting for a human.
      # Leave blank to keep the manual ./fetch-key.sh flow.
      - HAIER_USERNAME=${HAIER_USERNAME:-}
      - HAIER_PASSWORD=${HAIER_PASSWORD:-}
      - HAIER_REGION=${HAIER_REGION:-20}
      # Shared secret the phone sends with each location post. The hub is already tailnet-only,
      # so this is a second lock rather than the only one; blank disables the check.
      - HOME_LOCATION_TOKEN=${HOME_LOCATION_TOKEN:-}
    volumes:
      - home_data:/data

volumes:
  n8n_data:
  caddy_data:
  caddy_config:
  caddy_logs:
  dashboard_data:
  couchdb_data:
  home_data:
