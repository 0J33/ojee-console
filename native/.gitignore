node_modules/
package-lock.json

# Generated by `npm run configure` from .env — it holds YOUR tailnet origin,
# which is deployment configuration, not source.
capacitor.config.json

# Generated by `npx cap add`. Both are large, both are reproducible, and both
# contain machine-specific signing paths.
ios/
android/
